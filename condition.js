const year = number(prompt("Enter a year: "))
if ( year % 400 = 0 ) {
    console.log("${year} is a leap year.")
} else { 
    console.log("${year} is not a leap year.")
} // ? nereye noktalama işareti anlamadım